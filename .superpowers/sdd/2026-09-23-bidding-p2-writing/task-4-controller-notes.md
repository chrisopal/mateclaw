# Task4 implementation coordination preflight (Sol read-only)

Task4 awaits approved Task3, do not implement yet. Sol checked current scoped revision/head/decision architecture and found following necessary contracts (see BiddingSourceService65-78/227-232, AnalysisService274-278, OutlineService100-104, Repository409-411, Materials80-89, Handoff103-115).

1. Reverse traversal starts at OLD replaced Ref, persist old->new event. Source upload currently passes new Ref, so exact matching alone would miss old descendants. Supplement without supersedes conservatively blocks formal progress pending impact review.
2. Hook selected head replacement for sourceSet / baseline / outline / selected chapter, not every candidate insertion. Task3 final service must supply old/new exact head for chapters/manuscript. Decisions idempotent and changes within scoped project transaction.
3. Material binding/receiving a new presales version ADDS a selectable frozen snapshot; it does not silently replace existing material inputs. No automatic invalidation merely because another snapshot was added. Explicit chosen input/outline/chapter head replacement drives dependencies. ACL changes still revalidated on access; do not change original old snapshot bytes or globally blacklist project on an actor's403. Thus retain originalTask4 ownership unless a proven missing mutation hook requires declared additions.
4. Scoped reverse SQL must match kind/id/version/digest, NOT LIKE%id%. Resolve portable SQL for H2/MySQL/kingbase before implementation. Canonical server-written full Ref JSON token can be scoped SQL prefilter with LIKE wildcard escaping; post-check exact parsed Ref equality in returned matched rows so collation cannot prove false impacts. If choosing this portable representation, prove storage serialization/token consistency and conservatively classify noncanonical/missing dependency records unknown, never treat a false-negative as unaffected. Do not load full table into memory. Prefer existing tables and no new deps.
5. Reconfirm creates NEW immutable revision (same body bytes for proven unaffected) +new fixed input association +CAS head +decision. Never rewrite old payload/inputrefs, only mark old status invalid. Scope/projectlock and expected heads required.
6. unchangedRefs is a request, not proof. Resolutions must pin old/new inputs,targetRef,structureddecision/affected clause or conflict IDs, evidenceRefs(sourceRef+blockId). Verify evidence belongs to actual old/new input closures and covers every conflict. Missing dependency/mapping/proof =>unknown, formalBlockedtrue. Pure reason text cannot whitewash an affected technical/mandatory input.
7. Strong required evidence/material cannot be ACCEPT_RISKed away. Valid new material/proof or evidencedfalsepositive only; invocation ACL remains current actor/employee and scoped origin checks.

Task3 final head states, leaf requirement/material/evidence mappings are the load-bearing interface to review beforeTask4 dispatch. No P3 review/export functionality here.

Root UI integration preflight: Task5 checklist explicitly requires showing affected chapters and required confirmation for outline/supplement changes (plan line302). Task4 currently has no Controller ownership or authorized GET impact read model. Before Task4 dispatch, add BiddingController ownership and declare a scoped authorized read endpoint that exposes persisted pending changedRef/events and impact arrays; UI must not manufacture old/new refs or call a mutating confirm merely to read. Multiple pending changes must remain visible/blocked, not silently drop all but latest. Keep existing CONFIRM_CHANGE_IMPACT shape per-event and current approver gates. Exact read shape to coordinate after Task3 approved heads are final.

Semantic boundary: graph alone initially reaches baseline->outline->all chapters when sourceSet head changes. Selective recovery needs proved comparison of fixed old/new baseline technical/mandatory/scoring fields and each leaf's mapped inputs, not only graph non-membership or a free-text resolution. Deadline-only differences can permit new associations with identical body bytes; full approval remains invalid. Reconfirm copy must not recursively emit a new unresolved replacement event for the very same approved transition; insert new revisions/heads/decision as one controlled transaction. Never revive old review/artifact approval solely because bodyhash is unchanged.

Read API contract approved for implementation: GET /api/v1/bidding/projects/{id}/change-impact returns {events:[{eventId,changedRef,replacementRef,status,impact:{affectedRefs,unaffectedRefs,unknownRefs,formalBlocked}}],formalBlocked}. Events are persisted old->new transitions, status PENDING/CONFIRMED; no client-fabricated refs and no mutating query. Event detail may add server-derived clause/proof information if needed for actionable UI; document exact response before UI integration. CONFIRM_CHANGE_IMPACT remains per-event changedRef with unchangedRefs/resolutions, scoped project expectedRef and operation idempotency. Reader access and origin ACL recheck mandatory. Do not expose source content on revoked access. Repair analysis/outline candidate work may proceed; stale formal manuscript/adoption must stay blocked until proved reconfirmation. No arbitrary project-wide permanent block on a single actor.

Planned file-boundary amendment (commit only after Task3 approved): Task4 adds BiddingController.java for authenticated read API; Task5 adds BiddingChangeImpact.vue and biddingChangeImpact.test.ts for actual persisted event/impact display and explicit confirmation. No other ownership changes authorized.

Task3 final approved07c26d51: candidate activeinputRefs excludes previouschapter; frozen previous content/ref persists in private provenance and headGuard enforces completion/adoption. Do not treat private previousChapterRef as an invalidating active dependency of its fresh replacement. Authorized read model should supply short derived titles/labels for affected chapter/source refs, since current writing view may be unavailable during invalidation and UI must not display raw IDs. It may add refLabels:[{ref,title}] alongside the declared exact arrays. Never include revoked origin content in labels/proof payload.
