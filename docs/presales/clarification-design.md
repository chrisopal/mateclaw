# 需求澄清闭环

本轮推进 T10/A03：问题关联本项目需求、影响说明、工作区负责人、答复和来源记录；增加待处理/已答复筛选与详情展示。答复来源是人工记录的出处，不等同于经语义审核的证据，也不会设置客户确认或接受 Statement。

保存 ANSWERED 必须有答复和出处；关联需求必须存在于同项目，负责人使用现有工作区成员验证。G1 对旧记录也重新检查答复出处。允许带理由（问题与影响仍保留）重新打开问题，清除当前答复审计字段，历史项目修订保留。操作继续使用现有乐观锁与幂等，无新增表/依赖。

验证：服务端无来源/错误需求/非法负责人拒绝、答复回读与重开计数；前端缺字段不提交、信息显示；真实浏览器创建虚构问题→dashboard 待办→答复→刷新归零。

## 验收结果（2026-09-18）

后端 Presales 定向测试 14 项通过并成功打包；前端 16 项通过，局部 lint、类型检查、精度检查、标准构建通过。新增覆盖无出处拒绝、跨项目需求拒绝、非法负责人拒绝、服务器答复审计字段、重开与持久回读，前端缺出处不提交。

持久 H2 服务已重启加载最新包。真实浏览器在“启明制造 MES 试点（虚构验收）”创建返工批次澄清并关联既有需求：项目 v2→v3，dashboard 待澄清 0→1；通过优先跟进进入项目，记录虚构纪要出处并答复：v3→v4。刷新后答复/出处保留；待处理筛选为空、已答复为 1；dashboard 回读归零，客户确认仍为未确认。截图：../../output/presales/clarification-answered.png。

此次仅推进需求澄清，不代表所有 A03/T10 分支或真实 AI 已全量验收。来源文本为人工追溯记录，未经自动真实性校验；负责人选择仍是既有 ID 输入。未改生产、未自动提交。
