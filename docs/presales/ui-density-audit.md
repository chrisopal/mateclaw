# 售前工作台信息密度走查

## 调整蓝图

保留：业务数据、权限、员工执行入口、证据、版本、下载、修订、审批、状态相关错误。
调整：必要口径与审计标识按需展开；方案的版本标题/状态、操作下载、正文、需求响应分组留白。
删除：首页口号、卡片解释、点击教学、阶段页脚、台账说明，以及各页重复流程教学。

顺序：先通过现有19项回归；再减少常驻说明与重排方案；最后走查列表、六个页签和相关弹窗，检查桌面/窄屏及交互。

范围仅售前前端和本地UI技能，无后端和数据变更。

## 结果

- 修改 `PresalesDashboard.vue`、`PresalesWorkbench.vue`：删除重复说明，保留指标、待澄清数量、下载/修订/审批操作；必要规则采用原生 details/summary。
- 方案页：覆盖统计独立分组，版本标题与草稿状态相邻；下载独立成行；已确认需求标识按需查看；章节留白和正文1.7行高。
- 两个本地 skill（enterprise-ui-design、frontend-design）已追加常驻文案约束，enterprise checklist 增加信息负担检查。原版备份在 output/presales/skill-baseline；两个skill格式校验通过。
- 验证：修改前后均为19项前端测试通过；两个Vue文件ESLint、Snowflake检查、vue-tsc与Vite构建通过（原有大chunk警告仍在）；diff检查通过。
- 浏览器：六个详情页签、项目表单、方案修订表单、需求确认弹窗；1440px浅色/深色方案、390px首页与方案。页面scrollWidth与viewport均390，无页面横向溢出，宽表格内部滚动。查看定义通过Enter展开/收起，审批警示仍可见。
- 视觉判定93/100，证据见 output/presales/density-*.png。本轮仅呈现修改，不重新验证真实模型运行或发布业务；未部署生产。1366/1920、全量长内容和完整无障碍审计未覆盖。
