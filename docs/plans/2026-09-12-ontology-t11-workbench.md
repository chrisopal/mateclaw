# T11 升级迁移、停用与归档实施蓝图

基线 c7f4304c；semantic-m1 工作树、enterprise UI、原持久化库。

保留已有迁移准备/审核/执行/有条件回滚、图启停、版本可用性、历史证据及权限体系。补齐：

1. 版本页显示各知识库固定版本及启停状态；从所选版本进入差异或迁移。暂停后仍可查看历史。
2. 迁移页用业务阶段引导检查、确认、执行；区分显示名称变化和标识替换。回滚前检查执行时图版本，后续有变更则禁用并提示原因，后端继续作最终裁决。
3. 本体归档作为独立生命周期状态，保留历史，不自动删除或批量改写知识库。归档限制新增使用和模型维护；已有应用按显式启停控制。
4. 每个写入保持工作区隔离、版本/操作幂等；来源与已发布历史不重写。

验证：多个KB固定不同版本、名称变化与标识映射、迁移/回滚和后续写入拒绝回滚、启停历史可读、归档/恢复及越权拒绝；实际UI输入失焦/刷新与宽窄布局；原库重启回读。完成后本地Git提交，不推送。T12真实LLM与生产容量验收独立。
