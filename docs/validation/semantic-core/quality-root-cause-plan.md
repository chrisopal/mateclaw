# M1–M3 质量根因场景复核计划

日期：2026-09-08；起点：129a48cd。用户授权复核实现、架构、前端体验，并修复实际发现的问题。

## 边界和选择

采用现有独立核心、宿主服务、语义工作台，针对缺陷做增量修复；不替换成熟前端、不引入新依赖、不启动自动抽取或自动因果推理平台。保留 Wiki 资料权威、本体精确版本、不可变事实修订、显式证据和当前权限检查。修复既有链路比重写前端或增加新的分析引擎更容易验证、也更利于跟随上游。

## 场景

批次 LOT-QA-0908 出现尺寸超差，设备 CNC-07 执行加工。检验记录证实超差；初步调查提出“刀具磨损”和“夹具偏移”两个互斥根因候选。复核报告支持刀具磨损，管理员处理冲突后形成有证据的正式根因。发布本体 v2 不得改变仍绑定 v1 的图。撤回根因复核报告后，根因不再进入可信查询，批次、设备和已验证质量问题仍可追溯，历史保留。

这里验证的是企业事实与调查结论的治理闭环，不将关联自动当成因果，不将模型回答当成调查证据。

## 执行与验收

1. 并行进行后端/架构只读审查、前端审查及修复；主任务负责场景和集成。
2. 对可复现缺陷补反例，按权限、事实正确性、数据完整性、操作体验排序修复。
3. 认证 HTTP 测试覆盖本体发布/版本绑定、实体与来源、精确引用、候选隔离、审核权限、冲突与过期修订、邻域、根因撤回和部分证据仍有效。
4. 本地真实运行通过页面完成关键操作，API/数据库回读作证；截图检查工作台、审核、原文、空态、桌面与窄屏，保留现有主题。
5. Agent 明确授权后进行一次根因取证，核对 statement/revision/evidence；未证实假设不能作为可信事实。测试后恢复工具开关。
6. 运行受影响测试、前端类型/lint/build，最终记录修复、审查结论和实际未验证项。仅本地修复与验证，不自动推送本轮新提交。
