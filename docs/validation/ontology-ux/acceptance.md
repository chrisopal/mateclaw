# 工作台文案与本体关系图验收

日期：2026-09-08。基于 `34a5f7d23c7eefc095e92c3f89f62bb20d24421b` 的工作区修改。此为当时验收记录，随本次文档归档补充提交。

## 实现范围

- 知识工作台使用业务名称，并说明新增知识需要审核；后台状态、审核规则、权限及 API 不变。
- 本体版本详情展示 SVG 对象关系图：类型、属性摘要、有向关系、完整属性/关系详情、缩放和平移。
- 草稿编辑页提供折叠的关系图预览；已发布版本仍只读。保留原有编辑表格。
- 使用已有 Vue 与主题变量，无新增依赖。原生选择框提供键盘查看详情的入口。

## 验证

- 语义前端 19 个测试文件、54 项测试通过；更正文案测试类型后单独复跑该测试通过。
- vue-tsc、修改文件 ESLint、Snowflake 精度检查、git diff --check 通过。
- enterprise/classic 两种构建通过；原有大包警告仍存在。
- 真实 enterprise 页面：质量根因本体 v2 显示 4 类型、3 关系；质量问题显示 2 属性、mm、[-0.05,0.05]、调查状态枚举及入/出关系。
- 切换 v1 后选择清空，图不再显示 v2 调查状态属性，定义按所选版本更新。
- 1440px 浅色/深色、390px 布局完成截图检查；390px 无页面横向溢出。
- 图模型测试覆盖孤立类型、无效引用、平行/反向/自环、版本替换和 100 类型/500 关系完整保留。

## 证据与边界

截图位于 `output/playwright/ontology-ux/`，发布用截图位于 `docs/user-guide/ontology/images/16-ontology-structure.png` 和 `17-workbench-labels.png`。

手机端适配画布提供概览，阅读卡片可放大，完整文字也可用类型选择框查看。复杂图采用固定网格，关系密集时需缩放和平移；不提供自动语义分组。classic 本轮完成构建，浏览器截图验证使用 enterprise。未修改后台数据；本轮不重复 M1–M4 后端业务验收。
