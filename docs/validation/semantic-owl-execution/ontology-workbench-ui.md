# 本体建模工作台 UI 交付 — 2026-09-09

## 已实现

- 草稿默认打开本体结构视图，基本信息折叠；高级 OWL 文档/公理仍可访问。
- 已发布版本复用只读结构视图。
- 左侧搜索与定义目录，中间层级 SVG 图、缩放与键盘选中，右侧定义/关联公理；本体个体与概念结构分开。
- 以已解析公理为输入做有界展示投影，不凭签名共现制造边；punning 使用类型化 ID。
- 直接命名继承、定义域/值域及简单存在/全称限制可显示；复杂表达式保留高级视图及未完整投影计数。
- 新增概念、关系、数据属性表单调用现有原子编辑 API。未保存全文/忙碌时禁止表单操作；没有新后端协议或依赖。
- 复用企业主题、Element Plus、已有 dagre，调整连续面板与响应式布局。

## 改动文件

`ontology/OntologyEditor.vue`、`OntologyVersions.vue`；新增 `components/OntologyModelWorkbench.vue`、`ontologyProjection.ts`、`ontologyForm.ts` 和三组测试；中文/英文语言键。组件类型声明由现有自动生成器更新。

## 验证证据

- semantic 前端测试 81/81；定向 ESLint 和类型检查通过；enterprise 构建通过（保留 bundle 大小警告）。
- 在隔离环境通过真实 UI 新建“三坐标质量本体（建模示例）”，新增三个概念、两个关系、一个数据属性。刷新后 GET 回读 18 条公理、草稿修订号 7，包含使用测针关系；页面投影 7 个定义、6 条结构关联。
- 证据：`output/playwright/ontology-workbench-readback.json` 与同目录 desktop/mobile/dark PNG。
- 1440px 桌面和 390px 窄屏检查；窄屏页面级横向溢出为 false。深色通过真实主题开关检查。复杂公理投影/转义/punning/只读/禁用写入由回归测试保护。

## 边界

- 简单表单目前新增定义；既有标签修改、公理修改删除及复杂 OWL 建模仍走高级入口。
- 图仅为建模展示投影，不替代后端 OWL 语义校验，也不表示实际业务事实或必填要求。
- 测试示例是合成数据，未发布为业务本体。未执行工业业务效果实验。
- 当前 5189 Vite 已载入改动；生产构建已生成但未替换后端打包静态资源、未正式部署或提交合并。
- 未覆盖所有宽度和全站无障碍测量。
