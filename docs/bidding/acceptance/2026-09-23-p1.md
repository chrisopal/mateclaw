# P1 bidding acceptance record

This record separates automated checks from the live acceptance required to call the parsing baseline complete. Test fixtures and isolated databases do not by themselves establish production data or real-model quality.

## Static checks

- PASS — `python3 -m unittest discover -s scripts/bidding -p 'test_*.py'` (12 tests: manifest validation, no-create behavior, unsafe path rejection, backup completeness/checksum, remote probe declaration, and credential redaction).
- PASS — Python syntax compilation for `scripts/bidding/check-runtime.py` and `scripts/bidding/test_check_runtime.py`.
- PASS — source diff whitespace check (`git diff --check`).
- PASS — bidding UI ESLint/precision and Vue typecheck after the permission and 390px repairs; Vite production build completed with an existing large-chunk warning.

## Integration

- PASS — isolated H2 migrations run through V214 and SQL backup/restore read back a bidding project row. The test uses a temporary database and removes its files after completion; it leaves existing databases untouched.
- PASS — final Java 21 targeted `Bidding*Test,PresalesIntegrationTest` suite: 15 server reports / 117 tests / 0 failures / 0 errors / 0 skipped, using `-Dmaven.compiler.proc=full`. The focused `BiddingRecoveryTest` was also run separately: 3/3 passing.
- PASS — after the browser-found task-list fix, Java 21 `BiddingTaskTest,BiddingDashboardTest` reran with 22/22 passing; the focused bidding UI set reran with 21/21 passing.
- PASS — project-approval and viewer-read cross-boundary fixes passed 17/17 focused Java tests. The server-computed project approval flag is now consistent for member owners, viewer owners, and global admins; mounted workbench tests cover owner/nonowner action visibility.
- PASS (isolated copies only) — copied each of the two discovered H2 files to owner-only directories outside the checkout without opening or changing the originals. The runtime preflight accepted the copies and verified a checksum manifest for each backup. The first copy had one accessible workspace and zero bidding projects before the smoke test; the second copy also had zero bidding projects before its smoke test. Neither count is a production inventory.
- NOT_RUN — authoritative production business-data directory and production project count. The application's default H2 URL is relative to its process working directory, so these two copies cannot establish which historical path was the intended active business database.

## Browser

- PASS — final mounted bidding UI regressions: 25/25 component tests, including source selection/confirmation, task retry controls, workspace request cancellation, late responses, refreshed analysis state, localized task states, project-scoped approval controls, and successful tasks with no false failure alert.
- PARTIAL — real Playwright browser against the isolated copied H2 database: logged in, created a synthetic project with a real workspace member as owner, uploaded a synthetic DOCX, observed its asynchronous read, selected and confirmed the source, and refreshed the page. The source-set head returned V1 and the file remained readable after refresh. A deliberately interrupted first source read was retried successfully. At 1280 and 1440 CSS pixels, the source table and action buttons remained aligned; screenshots are in the owner-only isolated acceptance directory outside the checkout.
- PARTIAL — a second isolated copy retained an enabled DeepSeek configuration and three analyst-eligible employees. The browser bound an analyst, uploaded/read/confirmed the synthetic tender, dispatched all four analysis tasks, and retried only one failed task. API readback showed that task at two persisted attempts and the other three at one attempt each. A full service restart preserved all four tasks, attempt counts `[1,1,1,2]`, and source-set version V1. The browser recheck after the UI fix showed all four localized task names, status, attempt count and failure reason; the 1280px task drawer remained aligned with no console errors on reload. All four tasks failed, so structured analysis results, a confirmed baseline, and restart readback of that baseline were not demonstrated.
- PASS (layout only) — after the narrow-screen repair, real browser geometry at 390px had document width 390px and all four task cards ending within the viewport, with visible retry buttons and attempt details. At 1280px the desktop task tables remained in use. The final backend/UI reload showed project-owner controls for the authorized admin and no console errors. This does not replace a member-owner browser login or a successful model analysis.

## Real model and quality

- FAIL — the second isolated database contains an enabled DeepSeek model, but the provider startup probe returned HTTP 401. The provider was removed from the available pool; all four dispatched analysis attempts ended with `SKILL_NOT_LOADED` after the model could not produce a tool call. A retry of one task produced the same result while preserving its attempt history. This proves failure persistence and task-specific retry, not successful skill execution or structured output. No credential value was printed or stored in this report.
- NOT_RUN — four-category omission/misclassification scoring against an authorized, redacted real tender with golden labels. The only tender used in the browser was a clearly synthetic DOCX from the bid-agent reference corpus; no bid-agent runtime or code is linked into MateClaw.

## Dialect migrations

- PASS — H2 migrations through V214 ran in an isolated database.
- NOT_RUN — MySQL and Kingbase migration execution. No controlled instances were available; SQL files are not reported as runtime-verified.

## Backup and recovery

- PASS — isolated H2 `SCRIPT`/`RUNSCRIPT` recovery preserved the seeded bidding project row and migration history; temporary test files were removed.
- PASS — the runtime preflight rejects absent/empty existing files, unsafe data locations, missing or mismatched backup checksums, and does not create database files.
- NOT_RUN — production backup inventory, production restore, and business database readback. The actual business data directory and record count remain unverified, and the shared `output/presales/` data was deliberately left untouched.

## Acceptance status

The code and isolated H2 checks pass. Browser acceptance reaches source confirmation and failure/retry readback, but the DeepSeek 401 blocks all four skill results and the parsing baseline. Production data inventory, real-tender quality, and non-H2 dialect execution remain `NOT_RUN`. P1 parsing-baseline acceptance is **not complete**.
