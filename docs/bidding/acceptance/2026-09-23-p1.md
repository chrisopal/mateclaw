# P1 bidding acceptance record

This record separates automated checks from the live acceptance required to call the parsing baseline complete. Test fixtures and isolated databases are not evidence of production data, a real model result, or browser acceptance.

## Static checks

- PASS — `python3 -m unittest discover -s scripts/bidding -p 'test_*.py'` (12 tests: manifest validation, no-create behavior, unsafe path rejection, backup completeness/checksum, remote probe declaration, and credential redaction).
- PASS — Python syntax compilation for `scripts/bidding/check-runtime.py` and `scripts/bidding/test_check_runtime.py`.
- PASS — source diff whitespace check (`git diff --check`).

## Integration

- PASS — isolated H2 migrations run through V214 and SQL backup/restore read back a bidding project row. The test uses a temporary database and removes its files after completion; it leaves existing databases untouched.
- PASS — Java 21 targeted `Bidding*Test,PresalesIntegrationTest` suite: 17 report files / 117 tests / 0 failures / 0 errors / 0 skipped, using `-Dmaven.compiler.proc=full`. The focused `BiddingRecoveryTest` was also run separately: 3/3 passing.
- NOT_RUN — controlled existing business database preflight and bidding project count. `application.yml` uses a process-working-directory-relative H2 path. No verified business data directory was established from the available configuration. The only discovered database file is under the shared untracked `output/presales/` tree; it was not opened, copied, or modified. Do not infer that it is the bidding database.

## Browser

- PASS — mounted bidding UI regressions: 17/17 component tests, including source selection/confirmation, task retry controls, workspace request cancellation, late responses, and refreshed analysis state.
- NOT_RUN — real browser at 1280/1440 and authenticated server workflow. No app process was listening on the configured default port, the checked-in H2 path is relative to process working directory, and no isolated browser server/authenticated fixture was provisioned. The mounted UI tests do not establish layout in a real browser or DB-backed source/baseline readback.

## Real model and quality

- NOT_RUN — DeepSeek provider readiness, skill load summaries, persisted attempts/structured output, and four-category omission/misclassification scoring. The current shell has neither `DEEPSEEK_BASE_URL` nor `DEEPSEEK_API_KEY` set; no controlled database/provider readback was available. Historical validation notes do not prove current provider configuration. The available sample tender corpus is synthetic and cannot serve as a real authorized tender with golden labels.

## Dialect migrations

- PASS — H2 migrations through V214 ran in an isolated database.
- NOT_RUN — MySQL and Kingbase migration execution. No controlled instances were available; SQL files are not reported as runtime-verified.

## Backup and recovery

- PASS — isolated H2 `SCRIPT`/`RUNSCRIPT` recovery preserved the seeded bidding project row and migration history; temporary test files were removed.
- PASS — the runtime preflight rejects absent/empty existing files, unsafe data locations, missing or mismatched backup checksums, and does not create database files.
- NOT_RUN — production backup inventory, production restore, and business database readback. The actual business data directory and record count remain unverified, and the shared `output/presales/` data was deliberately left untouched.

## Acceptance status

The code and isolated H2 checks pass. Browser, live model, non-H2 dialect, and controlled business-data acceptance remain `NOT_RUN`; this record does not claim the P1 parsing baseline is complete.
