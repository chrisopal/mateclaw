package vip.mate.semantic.core.ontology;

/** Literal value types supported by the first semantic-core milestone. */
public enum ValueType {
    TEXT,
    DECIMAL,
    BOOLEAN,
    DATE,
    INSTANT
}
