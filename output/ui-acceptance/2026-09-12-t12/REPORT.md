# T12 本轮验收与修复

**状态：部分完成，T12/M3 尚未通过完整验收。** 本轮真实自然语言与资料建模主链路通过，修复两类业务名称展示问题；生产数据库、容量和完整 UI 组合仍有缺口。

## 已实际验证

- 设备自然语言建模：3 类对象、2 属性、1 关系和泵继承设备；2 个编号样例隔离，无臆造报警阈值。人工确认、检查、发布 v1、绑定知识库。
- 图书资料建模：完整读取固定资料，3 类对象、2 属性、2 关系，3 个实例隔离；不增加罚款或数量约束。22 条依据核对后检查通过、发布 v1。
- RDF/XML 经界面下载、导入新草稿：22 条规范表达式集合、OWL 文本及摘要相同。
- 一个真实模型样例经显式对象匹配和精确摘录提交：审核前 trusted=0，审核后 trusted=1；原文、码点、摘要及两次状态历史可回读。
- 65 项后端测试全部通过；40 项相关前端测试通过，ESLint、类型检查和 enterprise 构建通过。构建仍提示既有大分包警告。
- 既有备份的隔离副本 SQL 导出/恢复，39 张语义表数量和内容摘要一致；没有打开、重置或替换运行库。

## 实际模型用量

两轮均为 native 运行时的 deepseek/deepseek-chat，真实工具轨迹保留于 *-conversation.json。设备输入 103619、输出 1966、缓存读 25088 token；图书输入 126461、输出 2424、缓存读 34688 token。输入输出合计 234470 token，缓存读包含在供应商用量报告中，不另行相加。两轮工具均 completed。接口未提供账单金额，不能把 token 推算当实付费用，也不能据两个小样本声称整体准确率或成本达标。

## 本轮代码调整

- OntologyVersions.vue：将已有业务投影传给参考资料面板。
- ModelSampleReview.vue：复用现有名称解析，类型/关系显示业务名称。
- SemanticWorkbench.vue、StatementReviewDrawer.vue、statementLabel.ts：传递名称映射，保留规范标识用于实际断言；列表、审核和历史可读。
- 两份既有测试扩展名称回归。未新增依赖、数据库迁移或改动存储协议。

## 剩余验收门槛

1. 完整 UI 控件/角色/视口矩阵；资料更新、实体关系/属性/同一性、治理与升级回滚组合、智能体知识问答。本轮台账明确为不完整，历史测试不计入本轮 UI 覆盖。
2. MySQL/Kingbase 原生迁移全链：本机无可用服务，Docker daemon 不可达；脚本存在不是执行通过。
3. 生产文档量、单文档长度、实体/知识数、并发及延迟目标尚待确定，未进行生产容量压测。
4. 真实进程中断/导入恢复、供应商 429 与端到端限流未演练；当前通过的是租约、预算与长文档边界测试。备份恢复未证明实际故障切换的 RPO/RTO。

下一轮优先补第 1 项，再在隔离目标数据库上完成第 2、4 项，按用户确认规模执行第 3 项。不能提前将总体计划 T12 标记完成。

## 复跑与证据

在 semantic-m1 工作树运行 `python3 output/ui-acceptance/2026-09-12-t12/verify_readback.py`：只读持久化运行环境，对两任务、发布版本、OWL 往返、样例状态和证据做断言。它不重新生成模型，不替代 UI 验收。

UI 步骤见 ui-run.md；台账见 coverage.json；运行验证见 runtime/REPORT.md 和 runtime/maven-results.json；能力边界见 support-boundaries.md。prepare.py 会新建独立合成工作区，不能将其与已有固定 ID 回读脚本混用。所有数据保留在现有持久化库，未推送远程 Git。

## 2026-09-13 后续结果

上述优先项已继续执行，新增12个明确端到端场景完成、3处缺陷修复与重测，见 [续验报告](../2026-09-12-t12-e2e/REPORT.md)。本文件保留原批次结果，不将旧测试计入新批次；T12/M3整体仍受生产数据库、容量和故障演练等门槛限制。
