# 当前本体手册更新验收

日期：2026-09-13（Asia/Shanghai）。范围：业务操作手册、当前界面截图、离线阅读交互。产品基线提交：3cc62b81；本轮未修改产品源码。

## 交付

- 正文按本体概念、功能地图、固定版本应用和设备 P-101 操作案例组织。
- 16 张当天真实浏览器截图，统一 1600×1100，均已逐张检查。捕获时间和实际路径见 capture-result.json，原始页面文字见 current-*.txt。
- 主文使用当前案例；移除离线页中内嵌的历史附录。旧文件仍保留在仓库。
- 本地 index.html 内嵌截图和需求文本，不依赖网络资源。

## 结果

`node docs/user-guide/ontology/build.mjs` 成功。`node output/ui-acceptance/2026-09-13-manual/verify.cjs` 的 6 类文档检查通过：16 张嵌入图、17 个目录链接、16 次放大关闭、附件字节一致、390px 窄屏菜单与图片、打印媒体布局；1440px 桌面没有页面横向溢出，无 pageerror。视觉查看了桌面和窄屏输出。

readback-result.json 确认：未增加发布版本，知识库仍绑定 v1，唯一可信记录仍是 P-101 类型为泵的 r2，原证据不变。临时草稿已删除。

## 截图取证边界

开始建模、资料选择和样例核对为填表演示，没有再次提交建模任务或知识记录。发布检查是在从已发布 v1 建立的临时 v2 草稿上真实执行通过；没有发布 v2。建议图展示原任务已采用状态。问答图重新打开已保存的真实调用和回答，没有再次调用模型。迁移图仅展示另一个已保存演示图的入口，没有执行迁移。

采集脚本依赖既有案例和临时草稿前置条件。`readback.py` 中的清理逻辑只允许本轮固定 ID、版本与原模型内容一致的临时草稿，不允许删除其他草稿；之后运行仅回读无草稿状态。

采集调试中修正过知识库空格匹配、隐藏 combobox 的定位、样例重名匹配和过渡未完成截图；最终图已重拍。文档验证初次将空的预览图片也纳入 decode，纠正为正文图片后原断言通过。上述属于取证脚本修正，不计产品故障，也不扩张为全模块端到端验收。

## 复跑

前端 5189、后端 18109、原持久化验收库可用时，按 capture.cjs 顶部说明准备临时草稿后采集。日常仅重建/检查离线手册，无须启动业务服务：

```sh
node docs/user-guide/ontology/build.mjs
node output/ui-acceptance/2026-09-13-manual/verify.cjs
```

浏览器复用本机已存在的 Playwright 和 Chrome；更换机器时将 require 路径指向该机器已有 Playwright。剩余生产数据库、规模、故障恢复与完整交互矩阵的产品验收不属于本次文档 PASS。
