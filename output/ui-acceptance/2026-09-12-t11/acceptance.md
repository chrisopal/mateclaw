# T11 真实浏览器验收

环境：enterprise UI 5189 → 后端18109 → 原持久化 H2。使用隔离工作区 `T11 生命周期验收 20260912T131045Z`，未重置历史库。桌面Chromium，CSS视口390×844、768×960、1440×960、2560×960；不代表原生移动设备。

复跑准备：服务启动后运行 `python3 output/ui-acceptance/2026-09-12-t11/prepare.py` 生成新的隔离工作区，使用新 fixture.json 的ID替换下列对象。受控登录信息仅在内存中使用。原脚本输出中不包含令牌/口令。

| 用例/控件 | 操作与预期 | 结果 |
|---|---|---|
| 知识库应用 | 显示两个KB分别固定版本，升级一个不会改变另一个 | PASS：装配v3、维修v1 |
| 比较版本 | 维修行比较版本跳转 from=v1,target=v3，自动显示差异 | PASS |
| 准备升级入口 | 装配行以v2为目标进入升级tab，目标预选保持 | PASS |
| 同标识改名 | 不填映射，生成→批准→执行，0实体变化 | PASS |
| 安全回滚 | 没有后续写入时点击恢复原版本解释 | PASS，回到v1 |
| 标识替换 | 添加类映射Equipment→Machine，输入失焦后生成、批准、执行 | PASS，1实体变化 |
| 刷新 | 执行后刷新页面，v3与已执行计划保留 | PASS |
| 不安全回滚 | UI新增业务对象，再到升级tab | PASS，回滚禁用且短原因可见；API强行回滚409 |
| 暂停与恢复 | 应用行暂停，刷新，查看知识；恢复后再次刷新 | PASS |
| 归档 | 点击归档、确认、刷新；打开保留草稿 | PASS，模型归档、版本关闭、草稿只读 |
| 归档时既有KB | 已有v1 KB新增合成证据事实，再暂停并从历史打开证据 | PASS，保留事实r1/r2与原文，撤回禁用 |
| 恢复模型 | 恢复确认并刷新 | PASS，版本仍关闭、暂停KB仍暂停 |
| 显式重新开放 | 点击允许新绑定、恢复KB使用 | PASS，只有v3开放，其余仍关闭 |
| 窄屏 | 390px点击暂停/恢复、打开归档并取消 | PASS，操作在纵向列表内可见 |
| 宽屏 | 1440及2560检查内容宽度和操作对齐 | PASS，无固定窄幅留白和整页横向溢出 |
| 重启持久化 | 同库停止、备份、启动，运行readback.py --post-restart --reject-rollback | PASS，版本/计划/草稿/证据与文档摘要保留 |

证据：fixture.json、readback-archived.json、readback-restored.json（恢复后尚未手工开放版本）、post-restart.json（手工开放v3并恢复KB后）、history.json、visual-verdict.json，以及 *final.png、migration-390.png、paused-history-evidence.png。

过程中曾有原浏览器标签页不派发输入事件；新建同上下文验收标签页后恢复。未用直接修改前端store或localStorage替代UI行为。准备fixture与持久化读回使用API，升级批准执行、生命周期和启停由真实界面触发。

范围外：没有21个以上KB的分页浏览器样本；分页沿用现有服务，后续全产品验收补覆盖。没有真实LLM生成、原生MySQL/Kingbase或移动真机验证。暂停历史事实来自显式合成资料，并非LLM输出。
