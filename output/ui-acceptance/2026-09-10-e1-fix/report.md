# E1 焦点修复与复测

日期：2026-09-10。当前声明范围结论：**PASS**。修复前的 [FAIL 报告](../2026-09-10-e1/report.md) 和失败输出原样保留。

## 修复

- `OntologyModelWorkbench.vue`：关闭详情后等待 Vue 更新，再将焦点恢复至当前工作台的“查看定义详情”入口。每个实例使用自己的模板 ref；使用 preventScroll 避免额外滚动。
- `OntologyEditor.vue`、`OntologyVersions.vue`：来源面板支持程序化焦点；“查看公理与来源”跳转后焦点进入来源面板，避免停在隐藏检查器或丢到页面 body。保留原有来源聚焦及滚动行为。
- `ontologyModelWorkbench.test.ts`：新增关闭后焦点恢复测试，修复前 1 failed / 5 passed，修复后通过。

## 本次验证

- 原 32 项真实浏览器检查重新执行：草稿 16/16、已发布版本 16/16，全通过；写请求为 0。[结果](results.json)、[台账](coverage.json)、[原步骤复跑脚本](run.js)。
- 额外两页焦点链验证：目录节点自动打开 → 关闭后回到入口 → Enter 重开 → 公理来源跳转 → 来源面板取得焦点 → 清除聚焦成功。[脚本](focus.js)、[结果](focus-results.json)。修复前来源跳转的 `focusInSource` 为 false；修复后两页均为 SECTION / tabindex=-1 且抽屉隐藏。
- 14 个本体测试文件、60 项通过，见 [tests.log](tests.log)。TypeScript 与范围内 ESLint 通过，`git diff --check` 通过。
- 企业模式构建通过，保留既有大 chunk 警告，见 [build.log](build.log)。没有新增依赖。

台账审计 32 PASS、0 FAIL、无记录错误。额外焦点测试单独列出，未混入原 32 项计数。

## UI 测试技能改进

本地 `/Users/guojiexie/.codex/skills/ui-regression-acceptance/SKILL.md` 已补充：

1. 先用代表控件验证定位策略，针对组件库的可见标签或键盘操作，不直接把隐藏 input 点击超时视为产品缺陷。
2. 核对缩放等边界及前置状态；区分脚本错误、前置状态污染与产品断言失败，避免继发失败重复计数。
3. 普通关闭验证焦点恢复；跳转关闭验证目标焦点；不强制非模态面板使用模态焦点锁。
4. 截图等待相关布局/过渡稳定，拖拽与缩放重新读取坐标；交互抑制等待可观察状态。

这次既有技能已要求真实操作和焦点返回，问题也包括执行不充分；本次补充用于降低重复失误，不能保证自动穷尽所有界面状态。技能校验 `quick_validate.py` 通过（复用已有 Python 环境，没有安装项目依赖）。仅更新本地已安装技能，未推送技能仓库。

## 范围与限制

本轮修复的是 E1 焦点及相关跳转行为，未扩展到其他角色、触屏手势、原有发布/写表单或全部浏览器。产品代码及测试随本报告提交至当前分支。
