# T06 草稿逻辑检查验收

基线 51d61805。复用现有持久化库和受限推理进程。独立 T06 合成工作区，不创建正式图或发布版本。

- 正常模型：一致；没有无法成立的命名对象类型。
- 不可满足类型：整体可以一致，但 Sensor 类型不可能有实例，明确列出业务名称。
- 整体矛盾：owl:Thing 属于 owl:Nothing，不能显示通过。
- 模型实例矛盾：同一模型个体属于互斥类型，不能因仅检查 TBox 而漏过。
- 超时、失败、不支持：状态分别可识别，不转成成功。
- 检查期间修改草稿：服务端复核快照，前端也清理迟到结果。
- 取消/停止等待：必须准确描述实际停止范围，不宣称后台进程已取消。
- 不创建图、实体、正式版本；检查接口不改变草稿。
- 界面沿用检查发布页，包含开始、重试、停止等待、业务对象名称及宽窄屏状态；测试台账按实际控件补齐。

T07 的持久检查报告与统一发布门禁不在本批。
