# T05 验收范围

基线 5ecd93cd；测试环境为 5189 UI / 18109 API 与现有持久化 H2 库。合成数据在独立 T05 工作区，不清理或恢复原数据。

- 业务规则：对象选择、添加/编辑/取消/删除、必填/单值/单位/枚举，保存后刷新回读。
- 样例检查：完整记录缺失、单位错误、枚举越界、单值冲突、合法输入；部分信息缺失允许。
- 写入保护：草稿版本比较、幂等重试、权限/工作区隔离；样例不改变草稿或正式图实体。
- UI：下拉非首项选择与失焦、对话框关闭与焦点、结果字段业务名称、窄屏控件和页面横向溢出。
- 证据层：后端自动测试、前端组件测试与构建、真实浏览器操作、API 独立回读、服务重启后的持久化。
- 不包含 T06 草稿逻辑推理或 T07 统一发布门禁；检查成功仅表示这次业务规则样例通过。
