# T09 验收证据

- `verify_lifecycle.py`：现有持久化库上的12项API生命周期验收。运行 `python3 output/ui-acceptance/2026-09-11-t09/verify_lifecycle.py`，每次新建隔离合成工作区。
- `prepare_sample.py`：准备已接受的合成建模样例，不代表大模型生成效果。
- `browser-replay.js`：将函数交给已有 browser_run_code 工具执行。前提是管理员登录，并通过工作区菜单选择 sample-fixture.json 的工作区。脚本包含固定验收ID，重建fixture后需要同步ID；不重复提交事实。
- 首次通过真实UI提交样例事实2098323105969745921；审核2098311584749334530经过确认、撤回，最终r3。`post-restart.py`在重启后独立GET验证。
- `coverage.json`记录13项本轮核心交互。不宣称所有角色、全部条件字段写入和全站键盘可访问性穷尽。
- 初次脚本使用英文关闭标签、错误中文撤回文本而超时，按实际可访问标签修正后通过；一次回读URL遗漏/api/v1，纠正后回读成功。窄屏动画中间帧曾误采，等待布局和浮层稳定后重拍，不能据中间帧判断产品缺陷。
- 189项前端测试、后端测试、类型检查、ESLint及构建证据独立列在交付记录。
