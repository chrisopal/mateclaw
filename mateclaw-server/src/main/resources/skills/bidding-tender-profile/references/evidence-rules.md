# Evidence rules

Exact quotes are copied from the same source block whose read receipt exists for this attempt. Preserve source ID, version and block ID. If a value is absent or ambiguous, return `null` and explain why in `unknowns`; never fill gaps using outside knowledge.
